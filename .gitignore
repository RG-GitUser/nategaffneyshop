node_modules
server/node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.*
!.env.example
.vite

# local dev uploads (in-memory-DB test artifacts)
server/uploads-dev/
# paid PDFs — customer deliverables, never in the repo
server/private-pdfs/
